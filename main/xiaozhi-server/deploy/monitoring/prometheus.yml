global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/baize-alerts.yml

scrape_configs:
  - job_name: baize-app
    static_configs:
      - targets: [127.0.0.1:8003]

  - job_name: node
    static_configs:
      - targets: [127.0.0.1:9100]

  - job_name: blackbox-https
    metrics_path: /probe
    params:
      module: [http_2xx_tls]
    static_configs:
      - targets: [https://burningpowercat.com/healthz]
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: 127.0.0.1:9115
