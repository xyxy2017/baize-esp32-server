# Copy to /etc/baize/app-secrets.env, keep root-owned with mode 0600, and
# restart baize-xiaozhi. Never commit the real values.
ALIBABA_CLOUD_ACCESS_KEY_ID=replace_with_dedicated_sms_ram_key_id
ALIBABA_CLOUD_ACCESS_KEY_SECRET=replace_with_dedicated_sms_ram_key_secret
